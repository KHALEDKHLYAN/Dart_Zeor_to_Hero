/*
 Takes action as an Argument if condition is True, Nothing happens
 if the condition is False, It Will raise an error

 Syntax
 Assert(Condition)
 /OR
 assert(condition, "Your custome Message");
 */

void main() {
  var age = 22;
  assert(age != 22, "Age must be 22");
}
